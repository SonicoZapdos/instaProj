@model IEnumerable<instaProj.Models.Comment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Commeting)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContLike)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Archive)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ocult)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Commeting)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContLike)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Archive.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ocult)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
